<div id="<%= dom_id na_team %>">
  <%= turbo_frame_tag "na_team_#{na_team.id}" do %>
    <div class="na_team">
    
      <div class="grid  grid-cols-2 gap-2 justify-between items-center border border-blue-900 rounded-lg shadow-lg shadow-blue-900 hover:shadow-2xl mx-8 p-4">
        <div class="whitespace-nowrap">
            <span class="text-lg">  <%= link_to na_team.name, na_team_path(na_team) %></span>
            <span class="font-light text-sm"> - <%= na_team.city %>,  <%= na_team.state %></span>
        </div>

        <div>
        </div>

        <div class="na_team__actions">
          <%= button_to "Delete",
                      na_team_path(na_team),
                        method: :delete,
                        class: "font-semibold text-red-500" %>
          <%= link_to "Edit",
                      edit_na_team_path(na_team),
                      class: "font-semibold text-green-500" %>
          
        </div>
        <div>
                <%= link_to "(2) Add team members ", na_team_path(na_team), class: small_button_class,
                data: { turbo_frame: "_top" } %>
        </div>
    <br>
        <div class"block font-light text-gray-400 text-sm">
        <div class="font-light text-blue-900 ml-8 mb-4"> 
        <% na_team.members.each do |member| %>
          <%= member.full_name  %><br>
        <% end %>  
        </div>
              <% end %>
        </div>

</div>
</div>
</div>
