<% content_for(:page_title, "US Open Invites") %>

<div class="my-4">
  <%= link_to "Add Record", new_open_invite_path, class: form_button_class %>
</div>

<!-- 🔥 Scrollable container -->
<div class="overflow-x-auto w-full border rounded shadow bg-white">
  <!-- Header Row -->
  <div class="grid grid-cols-12 min-w-[1200px] bg-gray-200 border-b border-gray-400 px-2 py-2 text-sm text-center font-semibold">
    <div class="text-left ml-2">Person</div>  
    <div class="text-left ml-2">Status</div>
    <div class="text-center">Invite Sent</div>
    <div class="text-center">Invite Sent<br>Date</div>
    <div class="text-center">RSPV</div>
    <div class="text-center">Whova</div>
    <div class="text-center">Flight<br>Booked</div>
    <div class="text-center">Save The<br>Date</div>
    <div class="text-center">Response</div>
    <div class="text-center">Official<br>Invite</div>
    <div class="text-left">Comments</div>
    <div class="text-center">Actions</div>

  </div>

  <!-- Row Partials -->
  <% @open_invites.each do |invite| %>
    <%= render partial: "open_invite_row", locals: { open_invite: invite } %>
  <% end %>
</div>
