<% content_for(:page_title, "Edit: #{@person.full_name}")%>

<%= render "form", person: @person %>



<div>
  <%= link_to "Show this person", @person, class: link_class %> 
  <% if can?(:manage, Person) %>
  | <%= link_to "Back to People", people_path, class: link_class %>
<% end %>
</div>
