<div class="container mx-auto mb-24">
 
  <!-- Welcome message -->
  <div class="text-center p-6">
  <% if @member_type == "Club" && @club.present? %>
  <p class="mt-2 text-gray-700 text-lg">
    Are <span class="font-semibold"><%= @club.name %></span> ready for a new season? 🌟
  </p>
<% elsif @member_type == "Individual" && @individual_member.present? %>
  <p class="mt-2 text-gray-700 text-lg">
    Hi <%= @individual_member.first_name %>,<br>
    Ready for another netball season? 🌟
  </p>
<% else %>
  <p class="mt-2 text-gray-700 text-lg">
    Welcome to Netball America®! 🌟
  </p>
<% end %>
</div>
 <!-- CLOSE container -->

<!-- OUTSIDE container: full width banner -->
<% if @needs_renewal %>
  <div class="w-full bg-gradient-to-r from-blue-200 via-blue-100 to-blue-200 border-l-[10px] border-blue-600 text-blue-700 px-4 py-6 mb-8 shadow-lg mt-8 rounded-md">
    <div class="max-w-3xl mx-auto text-center flex flex-col items-center gap-4">

      <!-- Text -->
      <div>
        <h2 class="text-lg md:text-xl font-bold text-blue-700 mb-1">
          Membership Renewal
        </h2>
        <p class="text-gray-700 mb-4 text-sm md:text-base">
          Do you wish to renew your annual membership for the new season?
        </p>

        <div class="flex flex-wrap justify-center gap-4">
          <% if @member_type == "Club" %>
            <%= button_to "Yes, Renew!", renew_club_membership_path(@club, response: "yes"), method: :post, class: "bg-blue-700 hover:bg-blue-800 text-white font-semibold text-sm md:text-base py-2 px-5 rounded shadow-md transition-transform transform hover:scale-105" %>
            <%= button_to "No, Not Now", renew_club_membership_path(@club, response: "no"), method: :post, class: "bg-gray-400 hover:bg-gray-500 text-white font-semibold text-sm md:text-base py-2 px-5 rounded shadow-md transition-transform transform hover:scale-105" %>
          <% elsif @member_type == "Individual" %>
            <%= button_to "Yes, Renew!", renew_individual_membership_path(response: "yes"), method: :post, class: "bg-blue-700 hover:bg-blue-800 text-white font-semibold text-sm md:text-base py-2 px-5 rounded shadow-md transition-transform transform hover:scale-105" %>
            <%= button_to "No, Not Now", renew_individual_membership_path(response: "no"), method: :post, class: "bg-gray-400 hover:bg-gray-500 text-white font-semibold text-sm md:text-base py-2 px-5 rounded shadow-md transition-transform transform hover:scale-105" %>
          <% end %>
        </div>
      </div>

      <!-- Logo -->
      <div class="mt-2">
        <%= image_tag "Netball-America-Logo-Transparent-Background.png", alt: "Netball America® Logo", class: "h-16 w-auto" %>
      </div>

    </div>
  </div>
<% end %>

<div class="my-4">
    <%= render "pages/why_membership" %>
</div>











