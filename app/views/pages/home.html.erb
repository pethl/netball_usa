<% if Rails.env.development? %>
  <!-- DEV NOTE: Role configurations are defined in app/helpers/pages_helper.rb -->
<% end %>

<% if role_config %>
  <% content_for(:page_title, role_config[:title]) %>
  <%= render role_config[:partial] %>
<% else %>
  <% content_for(:page_title, "Welcome to Netball America®") %>
 
<% end %>