<main class="container">
  <%= link_to sanitize("&larr; Back to taems"), taems_path %>

  <div class="header">
    <h1>New taem</h1>
  </div>

  <%= turbo_frame_tag @taem do %>
    <%= render "form", taem: @taem %>
  <% end %>
</main>

