<% if user_signed_in? %>
 <% content_for(:page_title, "Educator")%>
  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
  <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
<% end %>

 <% unless user_signed_in? %>
   <br>
   <p class="text-center text-xl text-blue-900">Educator's Request for<br> More Information</p><br>
  <% end %>

<%= render @netball_educator %>

<br>
<% if user_signed_in? %>
  <div>
  <%= link_to "Edit this educator", edit_netball_educator_path(@netball_educator), class: block_link_class %> 
  <%= link_to "Back to netball educators", netball_educators_path, class: block_link_class %>

 
  <%= button_to "Delete this record", @netball_educator, method: :delete,
  form: { data: { turbo_confirm: "Are you sure?" } }, class: red_link_class %>

  <div class="my-4">
  <%= link_to "Add New Educator", new_netball_educator_path, class: form_button_class %>
  </div>

  </div>
  </div>
</div>
<% end %>

